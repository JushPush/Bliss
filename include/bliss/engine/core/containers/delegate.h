#pragma once

// TODO: Implement delegates (allow for an array of functions)

namespace Punji
{
    template <typename Func> struct delegate;
    template <typename Func> struct multicast_delegate;
}

